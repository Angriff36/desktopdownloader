name: Build Flutter Desktop App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.3'

      - name: Cache Flutter dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            frontend/.dart_tool
          key: ${{ runner.os }}-flutter-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.os }}-flutter-

      - name: Install dependencies
        run: flutter pub get
        working-directory: frontend

      - name: List files in frontend/lib
        run: dir /s frontend\lib

      - name: Build Windows executable
        run: flutter build windows --target=frontend/lib/main.dart
        working-directory: frontend

      - name: Zip Windows build
        run: |
          Compress-Archive -Path frontend\build\windows\runner\Release\* -DestinationPath frontend\build\windows\runner\x_video_downloader_windows.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: frontend/build/windows/runner/x_video_downloader_windows.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
